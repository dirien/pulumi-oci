// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import { input as inputs, output as outputs } from "./types";
import * as utilities from "./utilities";

/**
 * This data source provides details about a specific Rrset resource in Oracle Cloud Infrastructure DNS service.
 *
 * Gets a list of all records in the specified RRSet. The results are sorted by `recordHash` by default. For
 * private zones, the scope query parameter is required with a value of `PRIVATE`. When the zone name is
 * provided as a path parameter and `PRIVATE` is used for the scope query parameter then the viewId query
 * parameter is required.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testRrset = oci.GetDnsRrset({
 *     domain: _var.rrset_domain,
 *     rtype: _var.rrset_rtype,
 *     zoneNameOrId: oci_dns_zone.test_zone.id,
 *     compartmentId: _var.compartment_id,
 *     scope: _var.rrset_scope,
 *     viewId: oci_dns_view.test_view.id,
 * });
 * ```
 */
export function getDnsRrset(args: GetDnsRrsetArgs, opts?: pulumi.InvokeOptions): Promise<GetDnsRrsetResult> {
    if (!opts) {
        opts = {}
    }

    if (!opts.version) {
        opts.version = utilities.getVersion();
    }
    return pulumi.runtime.invoke("oci:index/getDnsRrset:GetDnsRrset", {
        "compartmentId": args.compartmentId,
        "domain": args.domain,
        "rtype": args.rtype,
        "scope": args.scope,
        "viewId": args.viewId,
        "zoneNameOrId": args.zoneNameOrId,
        "zoneVersion": args.zoneVersion,
    }, opts);
}

/**
 * A collection of arguments for invoking GetDnsRrset.
 */
export interface GetDnsRrsetArgs {
    /**
     * The OCID of the compartment the resource belongs to.
     */
    compartmentId?: string;
    /**
     * The target fully-qualified domain name (FQDN) within the target zone.
     */
    domain: string;
    /**
     * The type of the target RRSet within the target zone.
     */
    rtype: string;
    /**
     * Specifies to operate only on resources that have a matching DNS scope.
     * This value will be null for zones in the global DNS and `PRIVATE` when listing private Rrsets.
     */
    scope?: string;
    /**
     * The OCID of the view the resource is associated with.
     */
    viewId?: string;
    /**
     * The name or OCID of the target zone.
     */
    zoneNameOrId: string;
    /**
     * The version of the zone for which data is requested.
     */
    zoneVersion?: string;
}

/**
 * A collection of values returned by GetDnsRrset.
 */
export interface GetDnsRrsetResult {
    readonly compartmentId?: string;
    /**
     * The fully qualified domain name where the record can be located.
     */
    readonly domain: string;
    readonly id: string;
    readonly items: outputs.GetDnsRrsetItem[];
    /**
     * The type of DNS record, such as A or CNAME. For more information, see [Resource Record (RR) TYPEs](https://www.iana.org/assignments/dns-parameters/dns-parameters.xhtml#dns-parameters-4).
     */
    readonly rtype: string;
    readonly scope?: string;
    readonly viewId?: string;
    readonly zoneNameOrId: string;
    readonly zoneVersion?: string;
}
