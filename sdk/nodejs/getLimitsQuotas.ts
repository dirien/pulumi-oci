// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import { input as inputs, output as outputs } from "./types";
import * as utilities from "./utilities";

/**
 * This data source provides the list of Quotas in Oracle Cloud Infrastructure Limits service.
 *
 * Lists all quotas on resources from the given compartment.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testQuotas = oci.GetLimitsQuotas({
 *     compartmentId: _var.tenancy_ocid,
 *     name: _var.quota_name,
 *     state: _var.quota_state,
 * });
 * ```
 */
export function getLimitsQuotas(args: GetLimitsQuotasArgs, opts?: pulumi.InvokeOptions): Promise<GetLimitsQuotasResult> {
    if (!opts) {
        opts = {}
    }

    if (!opts.version) {
        opts.version = utilities.getVersion();
    }
    return pulumi.runtime.invoke("oci:index/getLimitsQuotas:GetLimitsQuotas", {
        "compartmentId": args.compartmentId,
        "filters": args.filters,
        "name": args.name,
        "state": args.state,
    }, opts);
}

/**
 * A collection of arguments for invoking GetLimitsQuotas.
 */
export interface GetLimitsQuotasArgs {
    /**
     * The OCID of the parent compartment (remember that the tenancy is simply the root compartment).
     */
    compartmentId: string;
    filters?: inputs.GetLimitsQuotasFilter[];
    /**
     * name
     */
    name?: string;
    /**
     * Filters returned quotas based on the given state.
     */
    state?: string;
}

/**
 * A collection of values returned by GetLimitsQuotas.
 */
export interface GetLimitsQuotasResult {
    /**
     * The OCID of the compartment containing the resource this quota applies to.
     */
    readonly compartmentId: string;
    readonly filters?: outputs.GetLimitsQuotasFilter[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The name you assign to the quota during creation. The name must be unique across all quotas in the tenancy and cannot be changed.
     */
    readonly name?: string;
    /**
     * The list of quotas.
     */
    readonly quotas: outputs.GetLimitsQuotasQuota[];
    /**
     * The quota's current state.
     */
    readonly state?: string;
}
